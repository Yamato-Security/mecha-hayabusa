
# Investigation scratch area used by the /investigate skill (datasets, batch
# JSON payloads, draft reports) — may hold customer log data, never commit it.
/work/

# MkDocs documentation site build output
/website/site/

# Python bytecode / caches
__pycache__/
*.py[cod]
