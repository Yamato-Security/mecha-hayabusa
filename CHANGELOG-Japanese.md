# 変更点

## 1.1.0 [2026/08/21]

**新機能:**

- evtxの識別子表現カバレッジ計測（`state.py reach --eid-metrics`）を追加した。収集したevtxの`(チャネル, イベントID)`のうち、提供されたタイムラインに一意に表現されないものを定量化し、CSVが完全な複製であると仮定しない。RecordIDを持つイベント行と、同一コーパスの候補グラフが強制する相関組み合わせは表現済みとして扱う。残る任意マッピングは下限・上限の範囲と、未確定な組み合わせの一覧として記録する。7,928ファイル・36.7GiBのコーパスでは、2,072組中1,959組、59,884,494イベント中43,981,154件（73.4%）がタイムラインの識別フィールドに存在せず、未確定な組み合わせは0組だった。`eid-metrics`はキーの両方を小文字化する一方、タイムラインは記録されたままの表記で出力するため、チャネルとイベントIDの両方を大文字小文字を無視して比較する。また、チャネル略称によって1つの表示名に集約される行（AppLockerの4サブチャネル、Security-Mitigationsの2チャネル）は、改竄されたファイルとみなさず合算する。整合しない通常イベントの組み合わせと具体的な相関投影は誤った数値を記録せず拒否する一方、空のEventIDは保守的に扱う。`eid-metrics`はEventIDキー自体が存在しないレコードを集計対象から除外し、値がnullのレコードは`null`として数えるため、そのような行は計測を破棄せず、該当し得る組み合わせを未確定として残す。公開する上下限は外側に丸め、0%や100%をまたがないため、1,000万件のコーパスにおける1件の欠落が「完全にカバーされている」と読めることも、1件だけ表現されている状態が「何も表現されていない」と読めることもない。 (#45) (@YamatoSecurity)
- 計測結果と曖昧性の範囲をレポート付録に出力するようにした。あわせて、時間・ホスト・レベル・ルールによる絞り込みは表現ギャップを過大評価し得るという注意書きと、元のevtxが利用できなかったことを明示的に記録する`reach --none --reason ...`を追加した。この比較が示すのは提供されたタイムラインにおける識別子の表現状況だけであり、「どのルールにも一致しなかった」「決して可視化できない」「ログに存在しない」ことは立証しない。`init`は記録済みの計測結果も他のステートと同様に初期化し、記録されたタイムラインのハッシュが読み込み中のデータセットと一致しない計測結果は、本調査のカバレッジとして公開せず保留する。 (#45) (@YamatoSecurity)

## 1.0.1 [2026/08/20]

**バグ修正:**

- 件数集約（correlation）系のHayabusaルールがトリアージ不可能となり、レポート生成そのものが阻害される問題を修正した。これらのルールは相関ウィンドウごとに1行の集約レコードを出力し`RecordID`が空になるが、`state.py`はデータセットに`RecordID`*カラム*が存在するだけで証拠refsを要求していたため、`indeterminate`を含むいかなる評決も記録できず、ゲートG1のpendingが0にならず、`report.py`が生成を拒否していた。トリアージエントリ、および当該ルールのみに依拠するfindingで`"refs_unavailable": true`を宣言できるようにした。ゲートはこの申告をデータセットと突き合わせて検証し、当該ルールにRecordIDを持つ行が存在する場合は拒否するため、行レベルの監査可能性は放棄されず維持される。 (#38 #39) (@YamatoSecurity)
- 件数集約ルールのdetailフィールドが一切抽出できない問題を修正した。これらのルールはフィールドを`Key:Value`形式（コロンの後に空白なし）で出力するため何も抽出できず、variant証拠、ひいては20件超のルールに対する`false_positive`評決が到達不能だった。新形式は通常規則が弾いたペアのみ、かつ2文字以上のフィールド名らしいキーのみに適用するフォールバックとしたので、ドライブレターがフィールドと誤認されることはなく、`C:\Windows\foo.exe`は従来どおり何も生成しない。3,254,344行のタイムラインで検証: 1,950行が新たにフィールドを獲得し、既存フィールドの変化は0件。 (#38 #39) (@YamatoSecurity)
- ホストカバレッジが疑似ホストを要求する問題を修正した。相関行は複数マシンをdetail区切り文字で連結した1つの`Computer`値に集約することがあり、それがゲートG2に単一の不正なホスト名として届いていた。構成ホストを個別に数えるようにした。 (#38 #39) (@YamatoSecurity)

## 1.0.0 [2026/07/31] - Black Hat Arsenal USA 2026 Release

最初の公開リリース。Mecha Hayabusaは、MCPサーバーと`investigate`スキルを提供し、AIアシスタントが
[Hayabusa](https://github.com/Yamato-Security/hayabusa)の結果を自然言語で調査し、DFIRタイムラインを
作成し、調査結果をレポートにまとめられるようにするツールである。ログデータはローカルのDuckDBファイル内に
留まる。

**新機能:**

- MCPサーバーと、英語版・日本語版の`investigate`スキルを追加した。これにより、AIアシスタントにHayabusaの結果を渡し、いずれの言語でも質問できるようになった。 (#2) (@pinksawtooth)
- カバレッジに基づく調査状態の追跡機能を追加した。これにより、何を調査済みで何が未調査かをアナリストが推測するのではなく、実行結果として報告されるようになった。 (#7) (@pinksawtooth)
- ラテラルムーブメント（伝播経路）を可視化するチャートを追加した。 (#3) (@nishikawaakira)
- `detail_source`オプションを追加し、デフォルトプロファイルに加えてHayabusaの`AllFieldInfo`プロファイルの出力も読み込めるようにした。 (#6) (@pinksawtooth)
- Material for MkDocsによる多言語対応のドキュメントサイトを追加し、GitHub Pagesで公開した。あわせてREADMEを、そのサイトへ誘導するランディングページに置き換えた。 (#4 #5) (@YamatoSecurity)
- `--db-path`を追加し、DuckDBの書き込みが1クライアントに限られることに由来するシングルクライアントモデルをドキュメント化した。 (#10 #19) (@YamatoSecurity)
- `correlate_lateral_movement`が、対象範囲が広すぎて処理しきれない実行については、完了できないラテラル結合を試みるのではなく実行を拒否し、どこを絞り込むべきかを示すようにした。 (#27 #31) (@YamatoSecurity)

**改善:**

- チャートスクリプトの入力を使用前に検証するようにし、生成されるスクリプトを対象とした振る舞いのテストを追加した。 (#13 #24) (@YamatoSecurity)
- スキルのインストール方法と`--dataset-root`をドキュメント化し、日本語READMEの言語切り替えを修正した。 (#14 #21) (@YamatoSecurity)
- ページネーション対応の各ツールで重複していたレスポンス末尾の処理を共通化した。 (#12 #25) (@YamatoSecurity)

**バグ修正:**

- チャートのツールチップが信頼できないログデータをエスケープせずに埋め込んでいたため、Hayabusaの結果に細工されたフィールド値が含まれていると、ブラウザで開いたチャートにマークアップを注入できた。ツールチップのデータをエスケープするようにした。 (#8 #17) (@YamatoSecurity)
- `decode_powershell_commands`がスキャンを途中で打ち切っていたため、打ち切り位置以降のエンコードされたコマンドが報告されず、「何も見つからなかった」ように見えていた。入力全体をスキャンするようにした。 (#9 #18) (@YamatoSecurity)
- `-EncodedCommand`の判定が狭すぎ、PowerShell自体が受け付ける表記を取りこぼしていたため、省略形や大文字小文字が異なる形式のエンコードされたペイロードがデコードされなかった。実行可能なすべての表記を認識するようにした。 (#28 #32) (@YamatoSecurity)
- ページネーション対応の各ツールが一意に定まらないキーで並び替えていたため、同じクエリでもページ間で行の重複や欠落が起こり得た。並び順を一意に定まるものにし、ページの結果を再現可能にした。 (#26 #30) (@YamatoSecurity)
- 英語版と日本語版のスキルでチャートのラベルに差異が生じていたため、ローカライズのずれを修正した。 (#11 #20) (@YamatoSecurity)

**その他:**

- プルリクエストではドキュメントのビルドを`--strict`で実行するようにし、各ワークフローのcheckoutアクションのバージョンを統一した。 (#16 #23) (@YamatoSecurity)
- `pyproject.toml`のプレースホルダーのままだったプロジェクト説明を記入した。 (#15 #22) (@YamatoSecurity)
- スキルが使用する`work/`スクラッチディレクトリを`.gitignore`に追加し、生成されたデータセットやレポートの下書きが誤ってコミットされないようにした。 (#29 #33) (@YamatoSecurity)
