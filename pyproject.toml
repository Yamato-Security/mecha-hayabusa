[project]
name = "hayabusa-mcp"
version = "1.0.0"
description = "AI-driven DFIR analyzer for Hayabusa results: an MCP server exposing read-only DuckDB analytics over Hayabusa CSV timelines, with an investigation skill for LLM-driven incident timeline and report generation."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "duckdb>=1.4.4",
    "mcp>=1.26.0",
    "pandas>=3.0.1",
]
